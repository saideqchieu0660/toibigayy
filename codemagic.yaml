workflows:
  android-debug:
    name: Android Debug Build
    max_build_duration: 60
    instance_type: linux
    environment:
      groups:
        - android_sdk
    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk
